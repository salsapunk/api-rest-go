domain = entidades
repository = fala com o banco
services = regras de negócio (orquestra as operações, lógica)
handler = lida com o http (lê o body, chama o service e devolve o json)
cmd/api/main.go = ponto de entrada
